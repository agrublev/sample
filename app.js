import React from "react";
import ReactDOM from "react-dom";
import {Button,Form,Input} from "./index";

ReactDOM.render(<div>Hey
<Button type="primary">Tesd</Button>
    <Form>
        <Input placeholder="TEst 52"/>
    </Form>
</div>, document.getElementById("mountNode"));
